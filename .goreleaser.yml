project_name: jarvis
release:
  github:
    owner: comigor
    name: jarvis-go
builds:
  - id: jarvis
    main: ./cmd/jarvis
    binary: jarvis
    goarch: [amd64, arm64]
    goos: [linux, darwin, windows]
    ldflags: -s -w

archives:
  - id: default
    format: tar.gz
    files:
      - jarvis

dockers:
  - image_templates:
      - ghcr.io/jarvis-go/jarvis:latest
    dockerfile: Dockerfile
    build_flag_templates:
      - "--platform=linux/amd64"
